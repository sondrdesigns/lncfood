/**
 * PostCSS Configuration for Next.js + Tailwind v4.
 * Replaces the Vite `@tailwindcss/vite` plugin with `@tailwindcss/postcss`.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
